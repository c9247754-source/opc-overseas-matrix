# OgMaker

Free Open Graph / social card generator (1200×630). Pure Sharp templates — no AI API cost.

## Local

```bash
cd apps/ogmaker
cp .env.example .env.local
npm install
npm run dev   # http://localhost:3003
```

## Creem

- Credits $9 → 100 clean exports  
- Pro $19/mo → 30 days  

Webhook: `/api/webhook/creem`

## Deploy (Vercel)

- Root Directory: `apps/ogmaker`
- Env: Creem keys + product IDs
- SEO tip: title/meta already target “Open Graph image generator”

## 14-day kill

0 paid + no organic search/PH interest → kill.
